# 🔐 نظام التسجيل والمصادقة

تم تحديث النظام ليشمل نظام تسجيل دخول منفصل لكل واجهة!

## 📱 الواجهات المتاحة

### 1️⃣ لوحة المنازل (Home Dashboard)
**الوصول:** `https://syntrue-absher.onrender.com/static/home-login.html`

**بيانات الدخول:**
- اسم المستخدم: `home`
- كلمة المرور: `home123`

**المميزات:**
- ✅ مراقبة الأجهزة المنزلية الأمنية
- ✅ خرائط تفاعلية للموقع
- ✅ تنبيهات فورية للأحداث
- ✅ إحصائيات وتقارير شاملة
- ✅ نظام حل المشكلات المتقدم
- ✅ تصدير واستيراد البيانات

---

### 2️⃣ لوحة العمليات (Operations Dashboard)
**الوصول:** `https://syntrue-absher.onrender.com/static/operations-login.html`

**بيانات الدخول:**
- الرقم العسكري: `ops`
- الرمز السري: `ops123`

**المميزات:**
- ✅ متابعة الدوريات الأمنية
- ✅ تتبع GPS للدوريات
- ✅ إدارة الحالات الطارئة
- ✅ تحليل أوقات الاستجابة
- ✅ مركز العمليات الميداني
- ✅ إحصائيات مفصلة للأداء

---

## 🚀 كيفية الاستخدام

### التشغيل الأولي:
```bash
# 1. تفعيل البيئة الافتراضية
.\.venv\Scripts\Activate.ps1

# 2. تشغيل الخادم
cd backend
python main.py
```

### تسجيل الدخول:

#### للمنازل:
1. افتح المتصفح على: `https://syntrue-absher.onrender.com`
2. سيتم توجيهك تلقائياً لصفحة تسجيل دخول المنازل
3. أدخل: `home` و `home123`
4. اضغط "تسجيل الدخول"

#### للعمليات:
1. افتح المتصفح على: `https://syntrue-absher.onrender.com/static/operations-login.html`
2. أدخل: `ops` و `ops123`
3. اضغط "دخول آمن"

---

## 🔒 نظام الأمان

### المميزات الأمنية:
- ✅ **فصل كامل بين الواجهات**: كل واجهة لها نظام مصادقة خاص
- ✅ **حفظ الجلسة**: يتم حفظ حالة تسجيل الدخول في المتصفح
- ✅ **خيار "تذكرني"**: حفظ اسم المستخدم للدخول السريع
- ✅ **حماية الصفحات**: لا يمكن الوصول للوحات بدون تسجيل دخول
- ✅ **تسجيل خروج آمن**: حذف جميع بيانات الجلسة عند الخروج

### تخزين البيانات:
- يتم حفظ حالة التسجيل في `localStorage` للمتصفح
- لوحة المنازل تستخدم: `homeAuth`, `homeUsername`
- لوحة العمليات تستخدم: `opsAuth`, `opsUsername`
- البيانات محلية ولا يتم إرسالها لأي خادم خارجي

---

## 🎨 التصميم

### صفحات تسجيل الدخول:
- **تصميم احترافي** مع تأثيرات بصرية جميلة
- **ألوان مميزة** لكل واجهة (أخضر للمنازل، أحمر للعمليات)
- **أيقونات Font Awesome** للمظهر الاحترافي
- **رسائل خطأ واضحة** عند إدخال بيانات خاطئة
- **تأثيرات حركية** (animations) عند التفاعل

### الواجهات الرئيسية:
- **عناوين مميزة** مع معلومات المستخدم
- **زر تسجيل خروج** واضح في كل صفحة
- **تصميم متجاوب** يعمل على جميع الأجهزة
- **ألوان وتدرجات جذابة**

---

## 🔧 التخصيص

### تغيير بيانات تسجيل الدخول:

#### لوحة المنازل (home-login.html):
ابحث عن السطر:
```javascript
if (username === 'home' && password === 'home123') {
```
غيّر `home` و `home123` حسب رغبتك

#### لوحة العمليات (operations-login.html):
ابحث عن السطر:
```javascript
if (username === 'ops' && password === 'ops123') {
```
غيّر `ops` و `ops123` حسب رغبتك

---

## 📂 ملفات النظام

### صفحات تسجيل الدخول:
- `backend/static/home-login.html` - تسجيل دخول المنازل
- `backend/static/operations-login.html` - تسجيل دخول العمليات

### اللوحات الرئيسية:
- `backend/static/dashboard.html` - لوحة المنازل (محمية)
- `backend/static/operations-dashboard.html` - لوحة العمليات (محمية)
- `backend/static/operations-center.html` - مركز العمليات (محمي)

---

## ⚠️ ملاحظات هامة

1. **الحماية الكاملة**: جميع اللوحات محمية ولا يمكن الوصول إليها بدون تسجيل دخول
2. **الجلسات المنفصلة**: يمكن تسجيل الدخول في كلا الواجهتين في نفس الوقت
3. **التوجيه التلقائي**: عند زيارة الصفحة الرئيسية `/` سيتم توجيهك للوحة المنازل
4. **الروابط بين اللوحات**: عند الضغط على رابط لوحة أخرى، سيتم توجيهك لصفحة تسجيل الدخول الخاصة بها

---

## 🆘 استكشاف الأخطاء

### لا أستطيع تسجيل الدخول:
- تأكد من إدخال البيانات الصحيحة (راجع `LOGIN_CREDENTIALS.txt`)
- تأكد من تشغيل الخادم على `https://syntrue-absher.onrender.com`
- امسح بيانات المتصفح (localStorage) وحاول مرة أخرى

### تم تسجيل الخروج تلقائياً:
- قد تكون بيانات الجلسة قد حُذفت
- أعد تسجيل الدخول مرة أخرى

### الصفحة لا تحمّل:
- تأكد من تشغيل الخادم
- تحقق من العنوان في المتصفح
- تأكد من عدم وجود أخطاء في console

---

## 📞 الدعم

لمزيد من المعلومات، راجع:
- `LOGIN_CREDENTIALS.txt` - بيانات تسجيل الدخول
- `QUICK-START.md` - دليل البدء السريع
- `USAGE_GUIDE.txt` - دليل الاستخدام الشامل

---

✨ **تم التحديث بنجاح!** النظام الآن يحتوي على:
- 🔐 نظام مصادقة كامل
- 🎨 واجهات جميلة واحترافية
- 🛡️ حماية شاملة للبيانات
- 📊 فصل تام بين الواجهتين

**استمتع باستخدام النظام!** 🚀
