# نظام الأمان الذكي - نسخة أبشر المحسّنة 🚔

## نظرة عامة

نظام أمني متكامل لتتبع الدوريات الأمنية ومراقبة الحوادث في الوقت الفعلي مع دعم GPS، إشعارات صوتية، وقاعدة بيانات دائمة.

## ✨ الميزات الجديدة (v2.0)

### 1. 🔔 نظام الإشعارات الصوتية
- تنبيهات صوتية تلقائية عند حالات الخطر
- أصوات مختلفة لكل نوع حالة (آمن، تحذير، خطر)
- صوت سيرين متعدد النغمات للطوارئ

### 2. 📱 تطبيق PWA (Progressive Web App)
- تثبيت التطبيق على الشاشة الرئيسية للجوال
- يعمل بدون إنترنت (Service Worker)
- أيقونات احترافية بجميع المقاسات
- دعم Push Notifications

### 3. 📍 GPS الحقيقي
- التقاط موقع GPS الفعلي من جهاز العسكري
- إرسال الإحداثيات مع كل حالة
- عرض دقة GPS المستخدمة
- تحديث تلقائي للموقع

### 4. 💾 قاعدة بيانات SQLite
- حفظ دائم للأحداث والبيانات
- جداول للعسكر، المستخدمين، المناطق الخطرة
- سجل نشاطات كامل
- استعلامات سريعة ومحسّنة

### 5. 📊 إحصائيات متقدمة
- API جديد للإحصائيات `/api/statistics`
- تقارير حسب المستوى والحالة
- إحصائيات الـ 24 ساعة الأخيرة
- عدد العسكر النشطين

## 🗂️ هيكل المشروع

```
smart-security-absher/
├── backend/
│   ├── main.py              # FastAPI Server محسّن
│   ├── database.py          # نظام قاعدة البيانات SQLite
│   ├── smart_security.db    # ملف قاعدة البيانات (يُنشأ تلقائيًا)
│   ├── requirements.txt
│   └── static/
│       ├── dashboard-absher.html      # الداشبورد الرئيسي
│       ├── operations-center.html     # مركز العمليات
│       ├── officer-device.html        # جهاز العسكري (PWA)
│       ├── analytics.html             # التحليلات
│       ├── manifest.json              # PWA Manifest
│       ├── service-worker.js          # Service Worker
│       ├── icon-generator.html        # مولد الأيقونات
│       └── icons/                     # أيقونات PWA
```

## 🚀 التشغيل السريع

### 1. تثبيت المتطلبات
```bash
cd backend
pip install -r requirements.txt
```

### 2. تشغيل السيرفر
```bash
python main.py
```

أو استخدم ملفات التشغيل السريع:
- **Windows**: `run.bat` أو `run.ps1`

### 3. الوصول للنظام

| الواجهة | الرابط |
|---------|--------|
| 📊 الداشبورد الرئيسي | https://syntrue-absher.onrender.com |
| 🗺️ مركز العمليات | https://syntrue-absher.onrender.com/static/operations-center.html |
| 📱 جهاز العسكري | https://syntrue-absher.onrender.com/static/officer-device.html?id=officer_1 |
| 📈 التحليلات | https://syntrue-absher.onrender.com/static/analytics.html |
| 🎨 مولد الأيقونات | https://syntrue-absher.onrender.com/static/icon-generator.html |

## 📱 تثبيت PWA على الجوال

### Android Chrome:
1. افتح `https://syntrue-absher.onrender.com/static/officer-device.html?id=officer_X`
2. اضغط القائمة (⋮) > "Add to Home screen"
3. اكتمل التثبيت!

### iOS Safari:
1. افتح الرابط في Safari
2. اضغط زر المشاركة
3. "Add to Home Screen"

## 🔌 API Endpoints الجديدة

### Events
- `GET /api/events?limit=1000&status=open` - جلب الأحداث
- `POST /api/events` - إضافة حدث جديد (مع GPS)
- `POST /api/events/resolve_all` - حل كل الأحداث
- `POST /api/events/reset` - مسح كل الأحداث

### Officers
- `GET /api/officers` - جلب كل العسكر
- `POST /api/officers` - إضافة عسكري جديد
- `GET /api/officers/{id}` - جلب عسكري محدد

### Statistics
- `GET /api/statistics` - إحصائيات شاملة

### Health
- `GET /health` - فحص صحة النظام

## 📊 قاعدة البيانات

### الجداول الأساسية:

#### `events` - الأحداث
```sql
- id, timestamp, device_id, type, level
- status, home_id, absher_id, location
```

#### `officers` - العسكر
```sql
- id, name, badge_number, rank
- phone, email, status
```

#### `users` - المستخدمين
```sql
- id, username, password_hash, role
- officer_id, is_active
```

#### `danger_zones` - المناطق الخطرة
```sql
- id, name, center_lat, center_lng
- radius, severity, is_active
```

#### `messages` - الرسائل الداخلية
```sql
- id, from_user_id, to_user_id
- subject, body, is_broadcast
```

#### `activity_log` - سجل النشاطات
```sql
- id, user_id, action
- entity_type, entity_id, details
```

## 🎯 الميزات القادمة

- [ ] نظام تسجيل الدخول والصلاحيات
- [ ] تقارير PDF تلقائية
- [ ] رسم مناطق الخطر على الخريطة
- [ ] نظام الرسائل الداخلية (WebSocket Chat)
- [ ] لوحة إدارة متقدمة
- [ ] ذكاء اصطناعي للتنبؤ بالمخاطر
- [ ] دمج كاميرات المراقبة
- [ ] دعم الطائرات بدون طيار

## 🔧 التطوير

### إضافة عسكري جديد برمجياً
```python
import requests

officer_data = {
    "id": "officer_6",
    "name": "الرقيب فهد الشمري",
    "badge_number": "1006",
    "rank": "رقيب",
    "phone": "+966501234567"
}

response = requests.post("https://syntrue-absher.onrender.com/api/officers", json=officer_data)
print(response.json())
```

### إرسال حدث مع GPS
```javascript
const event = {
  device_id: "officer_1",
  type: "officer_emergency",
  level: "danger",
  location: {
    lat: 24.7136,
    lng: 46.6753,
    accuracy: 10
  }
};

fetch('https://syntrue-absher.onrender.com/api/events', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(event)
});
```

## 🛠️ استكشاف الأخطاء

### قاعدة البيانات لا تُنشأ؟
```bash
cd backend
python -c "import database; database.init_database()"
```

### الصوت لا يعمل؟
- تحقق من أن المتصفح يسمح بتشغيل الصوت
- اضغط في أي مكان بالصفحة أولاً (سياسة المتصفحات)

### GPS لا يعمل؟
- تحقق من أن المتصفح يطلب صلاحية الموقع
- استخدم HTTPS في الإنتاج (localhost يعمل بدون HTTPS)

## 📝 الترخيص

MIT License - استخدم المشروع بحرية!

## 👨‍💻 المطور

تم التطوير بواسطة GitHub Copilot
النسخة: 2.0 Enhanced Edition
التاريخ: ديسمبر 2025

---

**💡 نصيحة**: لإنشاء الأيقونات، افتح `/static/icon-generator.html` واضغط "Download All Icons"
