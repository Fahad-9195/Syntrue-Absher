# 🔒 لوحة الأمان الذكي - Smart Security Dashboard

نظام محاكاة متقدم لمراقبة أجهزة الأمان المنزلية والدوريات والدرون في منصة أبشر.

## 🚀 البدء السريع

### الطريقة 1: عبر ملف Batch (الأسهل)
انقر مزدوج على الملف:
```
run.bat
```

### الطريقة 2: عبر PowerShell
اضغط على اختصار Windows + X واختر PowerShell، ثم:
```powershell
cd C:\Users\abdal\Desktop\smart-security-absher
.\run.ps1
```

### الطريقة 3: يدوياً
```bash
cd backend
.\.venv\Scripts\activate
pip install -r requirements.txt
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## 📱 الروابط المهمة

بعد تشغيل الخادم، افتح هذه الروابط في المتصفح:

| الاسم | الرابط | الوصف |
|------|-------|-------|
| 📊 لوحة التحكم (الجديدة) | https://syntrue-absher.onrender.com/static/dashboard-simple.html | واجهة بسيطة وسهلة مع خريطة وأحداث |
| 📊 لوحة التحكم (الأصلية) | https://syntrue-absher.onrender.com/static/dashboard.html | واجهة متقدمة مع ميزات إضافية |
| 📚 توثيق API | https://syntrue-absher.onrender.com/docs | توثيق تفاعلي (Swagger) |
| 🔧 API المباشر | https://syntrue-absher.onrender.com/api/events | قائمة الأحداث بصيغة JSON |

## 🎯 الميزات الرئيسية

### 1. لوحة التحكم الحديثة
- ✅ خريطة تفاعلية لعرض مواقع الأجهزة
- ✅ مخطط بياني حي يتحدث كل 3 ثواني
- ✅ جدول أحداث مع فلاتر
- ✅ إحصائيات فورية
- ✅ مؤشر اتصال حي

### 2. نظام الأحداث
- 🚪 كواشف الأبواب
- 📹 كاميرات المراقبة
- 💨 كواشف الغازات
- 🚨 كواشف الحركة
- 👮 الدوريات السرية
- 🛸 الدرون

### 3. المحاكاة
اضغط على **▶️ تشغيل محاكاة** لإضافة أحداث عشوائية للاختبار.

### 4. مستويات الخطورة
- 🟢 **معلومة** (info) - أحداث عادية
- 🟡 **تحذير** (warning) - احذر من هذا
- 🔴 **خطر** (danger) - تنبيه فوري

## 📋 بنية المشروع

```
smart-security-absher/
├── backend/
│   ├── main.py           # API الرئيسي (FastAPI)
│   ├── requirements.txt   # المتطلبات
│   ├── static/           # الملفات الثابتة
│   │   ├── dashboard-simple.html  # لوحة التحكم الجديدة
│   │   ├── dashboard.html         # لوحة التحكم الأصلية
│   │   ├── app.js                 # JavaScript
│   │   └── styles.css             # التنسيقات
│   └── .venv/            # البيئة الافتراضية
├── dashboard/            # نسخة قديمة (لا تستخدم)
├── devices_simulation/   # محاكاة الأجهزة
├── run.bat              # ملف تشغيل Windows
└── run.ps1              # ملف تشغيل PowerShell
```

## 🔌 API Endpoints

### الحصول على الأحداث
```bash
GET /api/events
```

### إضافة حدث جديد
```bash
POST /api/events
Content-Type: application/json

{
  "device_id": "door_sensor_1",
  "type": "door_open",
  "level": "warning",
  "timestamp": "2025-12-10T14:30:00Z",
  "status": "open",
  "home_id": "HOME-1234",
  "absher_id": "1234567890"
}
```

### تعليم جميع الأحداث كـ تم الحل
```bash
POST /api/events/resolve_all
```

### مسح جميع الأحداث
```bash
POST /api/events/reset
POST /reset
```

## 🛠️ المتطلبات

- Python 3.8+
- FastAPI
- Uvicorn
- Leaflet.js (مكتبة JavaScript)
- Chart.js (مكتبة JavaScript)

## 📝 الملاحظات

- البيانات تُخزن في الذاكرة فقط (تُحذف عند إعادة تشغيل الخادم)
- لا توجد قاعدة بيانات
- الخريطة توضح مواقع افتراضية في الرياض

## 🎓 كيفية الاستخدام

1. **افتح لوحة التحكم:**
   ```
   https://syntrue-absher.onrender.com/static/dashboard-simple.html
   ```

2. **اضغط على "▶️ تشغيل محاكاة"** لإضافة أحداث عشوائية

3. **شاهد الأحداث تظهر على:**
   - الخريطة الحية
   - المخطط البياني
   - جدول الأحداث

4. **جرّب الفلاتر** لعرض أنواع محددة من الأحداث

## 🐛 حل المشاكل

### المشكلة: "لا يمكن الاتصال بـ https://syntrue-absher.onrender.com"
**الحل:** تأكد من أن الخادم يعمل (اترجم الناتج في الـ terminal)

### المشكلة: "خطأ في تثبيت المتطلبات"
**الحل:** جرب:
```powershell
python -m pip install --upgrade pip
pip install fastapi uvicorn
```

### المشكلة: "البيئة الافتراضية لا تعمل"
**الحل:** احذفها وأعد الإنشاء:
```powershell
rmdir .venv /s /q
python -m venv .venv
```

## 📞 الدعم

إذا واجهت مشكلة، تأكد من:
- ✅ Python مثبت على الجهاز
- ✅ لست داخل VPN أو Firewall محدود
- ✅ المنفذ 8000 غير مستخدم

## 📄 الترخيص

MIT License

---

**صُنع بـ ❤️ لمنصة أبشر**
